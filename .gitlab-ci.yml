image: node:alpine

services:
  - mongo

stages:
  - test

server_test:
  stage: test
  before_script:
    - cd backend
    - npm install
  script:
    - npm test
  except:
    changes:
      - ".gitlab-ci.yml"
      - "README.md"
      - "backend/docs/*"