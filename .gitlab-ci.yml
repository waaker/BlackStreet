image: node:alpine

services:
  - mongo

stages:
  - test

server_test:
  stage: test
  before_script:
    - cd server
    - npm install
  script:
    - npm test
  except:
    changes:
      - ".gitlab-ci.yml"
      - "README.md"
      - "server/docs/*"