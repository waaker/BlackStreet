image: node:alpine

services:
  - mongo

stages:
  - test

backend_test:
  stage: test
  before_script:
    - cd backend
    - npm install
  script:
    - npm test
  except:
    changes:
      - "README.md"
      - "backend/docs/*"
  only:
    - develop
    - backend